image: node:14

pipelines:
  default:
    - step:
        name: Run Test
        caches:
          - node
        script:
          - yarn install
          - echo This is where we run the test
          - yarn run test
  branches:
    develop:
      - step:
          name: Build
          caches:
            - node
          script:
            - echo installing...
            - yarn install
            - echo setting production environment variables
            - sed -i -e 's%$PROD_CLIENT_ID%'"$PROD_CLIENT_ID"'%g' ./env.production
            - sed -i -e 's%$PROD_CLIENT_SECRET%'"$PROD_CLIENT_SECRET"'%g' ./env.production
            - sed -i -e 's%$PROD_SERVER_URL%'"$PROD_SERVER_URL"'%g' ./env.production
            # - cp ./env.production ./env.development
            - echo lint...
            - yarn run lint
            - echo test...
            - >
              wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | apt-key add - && \
              sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list' && \
              apt-get update && \
              apt-get install -y google-chrome-stable xvfb procps 
            - yarn run test
            - echo Building production version...
            - npm run build --prod --output-hashing=all
            # improve performance of app https://angular.io/guide/deployment#measure-performance
          artifacts:
            - build/**
            - node_modules/** # Save modules for next steps
      - step:
          deployment: Development
          name: Deploy To Shared Host
          script:
            - pipe: atlassian/ftp-deploy:0.6.1
              variables:
                USER: $FTP_USERNAME
                PASSWORD: $FTP_PASSWORD
                SERVER: $FTP_HOST
                REMOTE_PATH: $FTP_REMOTE_PATH
                LOCAL_PATH: build
                SET_ARGS: "ssl:verify-certificate false"
                EXTRA_ARGS: "--transfer-all"
      - step:
          deployment: Development
          name: Deploy To AWS
          script:
            - pipe: atlassian/aws-s3-deploy:0.3.8
              variables:
                AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY
                AWS_SECRET_ACCESS_KEY: $AWS_SECRET_KEY
                AWS_DEFAULT_REGION: $REGION
                S3_BUCKET: $BUCKET_NAME
                LOCAL_PATH: build
                ACL: public-read
    # master:
    #   - step:
    #       name: Build
    #       caches:
    #         - node
    #       script:
    #         - yarn install
    #         - yarn run build
    #       artifacts:
    #         - dist/**
    #   - step:
    #       deployment: Production
    #       name: Deploy
    #       script:
    #         - echo deploying application to production environment
